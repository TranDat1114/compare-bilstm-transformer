# Cài torch theo nền tảng của bạn. Ví dụ GPU CUDA 12.4:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# CPU-only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
pyvi>=0.1.1          # tách từ tiếng Việt (tuỳ chọn, cho ablation word_segment)
# Notebook
jupyter
nbconvert
ipykernel
nbformat
